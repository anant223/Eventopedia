import React from 'react'
import Logo from "../Logo"

const LoadingSpinner = () => {
  return (
    <div className="loader absolute inset-0 bg-background w-full min-h-screen flex items-center justify-center text-text"></div>
  );
}

export default LoadingSpinner